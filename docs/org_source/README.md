# org_source

这里保存原项目的 org-mode 文档源、BibTeX 和导出的 HTML。它们用于追溯原始文档和 literate programming 来源，不是当前 MATLAB 活动代码入口。

当前活动代码入口见：

- `opt_minimal/`：轨迹规划与优化。
- `controller/`：控制、PWM、UKF 和闭环验证。
- `matlab/`：顶层 Simscape/Simulink 模型。
